<resources>
    <!-- Compose draws the UI; this theme only covers the window before it does. -->
    <style name="Theme.AdBlocker" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:colorAccent">@color/brand</item>
    </style>
</resources>
